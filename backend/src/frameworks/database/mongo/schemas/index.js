const user = require("./auth.schema");
const appartement = require("./appartement.schema");
const client = require("./client.schema");
const paiement = require("./paiement.schema");

module.exports = {
  user,
  appartement,
  client,
  paiement,
};
